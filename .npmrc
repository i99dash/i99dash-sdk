# i99dash publishes to public npm. No private-registry config needed.
# (The old @i99dash/admin-sdk lived on GitHub Packages; the consolidation
# moves everything to public npm — see MIGRATING.md.)
auto-install-peers=true
strict-peer-dependencies=false
